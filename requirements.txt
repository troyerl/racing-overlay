PyQt6>=6.6
pyirsdk>=1.3.5
# psutil is optional. The CPU% / Memory% header-footer slots fall back to native
# OS calls (Windows ctypes / Linux /proc), but psutil enables them on macOS too.
# GPU% uses Windows PDH or optional nvidia-smi on Linux.
# WiFi fallback uses Windows wlanapi or Linux /proc/net/wireless.
psutil>=5.9
# Shared track maps (MongoDB Atlas). The [srv] extra pulls dnspython for
# mongodb+srv:// URIs. Imported lazily, so the app still runs without it.
pymongo[srv]>=4.6
# CA bundle for MongoDB Atlas TLS on macOS python.org builds (see track_store).
certifi>=2024.2.2
# Track Scan v2 HTML loop import (active-config layer).
beautifulsoup4>=4.12
svgpathtools>=1.6
